.text-center
  %h2 Users
  %br/
  %br/
  %br/
  .container5
    - @users.each do  |user|
      .card
        %h3
          = user.first_name
        %p= user.address
        = link_to "See", user_path(user)
        = link_to "Delete", user_path(user), method: :delete, data: { |
          confirm: "Are you sure?" }                                  |
      %br/
      %br/
